{
  "name": "survivor-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf dist .cache || true",
    "build:prod": "rm -rf dist && mkdir -p dist && rsync -a --exclude 'dist' --exclude '.git' --exclude 'node_modules' . dist/"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
